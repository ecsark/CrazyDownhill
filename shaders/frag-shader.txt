#version 120

uniform mat4x4 MVP;
varying vec3 outNorm, lightDir;

void main()
{
 // gl_FragColor = vec4(1, 0, 1, 1);
 //gl_FragColor = gl_Color;
gl_FragColor = gl_LightSource[0].diffuse * max(dot(outNorm, normalize(lightDir)),0.0) *gl_FrontMaterial.diffuse;
}
